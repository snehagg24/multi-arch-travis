language: bash

os: linux

services: docker

dist: bionic

jobs:
  include:
   - arch: amd64
     name: building x86_64 image 
     script: chmod +x ./build.sh; ./build.sh
   - arch: ppc64le
     name: building ppc64le image 
     script: chmod +x ./build.sh; ./build.sh
   - stage: building multi-arch using ppc64le as host
     arch: ppc64le
     script: chmod +x ./create-multi-arch.sh; ./create-multi-arch.sh
